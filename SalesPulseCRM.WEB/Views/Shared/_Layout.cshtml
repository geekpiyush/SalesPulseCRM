@{
    var userName = Context.Session.GetString("UserName") ?? "User";
    var role = Context.Session.GetString("UserRole") ?? "";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - CRM</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

 
    <link href="~/css/HomeController/Dashboard/Dashboard.css" rel="stylesheet" />
    <!-- Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

</head>

<body>

    <!-- SIDEBAR -->
    <div class="sidebar" id="sidebar">
        <h4 class="logo-text">🚀CRM </h4>

        <a asp-action="Index" asp-controller="Home" class="active">
            <i class="bi bi-speedometer2"></i>
            <span class="menu-text">Dashboard</span>
        </a>

        <div class="menu-group">
            <a class="menu-toggle">
                <i class="bi bi-people-fill"></i>
                <span class="menu-text">Leads</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">
                @if(role == "Admin" || role == "Manager")
                {
                    <a asp-action="CreateLead" asp-controller="Lead">
                        <span>Add Lead</span>
                    </a>
                }
                <a asp-action="Index" asp-controller="Lead">
                    <span>View Leads</span>
                </a>
            </div>
        </div>

        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-calendar2-check"></i>
                <span class="menu-text">Follow-ups</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">
                <a asp-action="Today" asp-controller="FollowUp">
                    <span>Today Follow-ups</span>
                </a>

                <a asp-action="Missed" asp-controller="FollowUp">
                    <span>Missed Follow-ups</span>
                    <span class="badge bg-danger ms-auto">3</span>
                </a>
            </div>
        </div>

        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-person-badge"></i>
                <span class="menu-text">Employees</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">
                @if(role == "Admin")
                {
                    <a asp-action="Register" asp-controller="Auth">
                        <span>Add Employee</span>
                    </a>

                    <a asp-action="Index" asp-controller="Users">
                        <span>View Employees</span>
                    </a>

                    <a asp-action="Performance" asp-controller="Users">
                        <span>Performance</span>
                    </a>
                }

             

            </div>
        </div>
        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-diagram-3-fill"></i>
                <span class="menu-text">Teams</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">
                @if(role == "Admin")
                {
                    <a asp-action="CreateTeam" asp-controller="Team">
                        <span>Add Team</span>
                    </a>
                }
             

                <a asp-action="ViewTeam" asp-controller="Team">
                    <span>View Teams</span>
                </a>

                <a asp-action="Performance" asp-controller="Team">
                    <span>Team Performance</span>
                </a>

            </div>
        </div>

        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-person-lines-fill"></i>
                <span class="menu-text">Customers</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">

                <a asp-action="Create" asp-controller="Customer">
                    <span>Add Customer</span>
                </a>

                <a asp-action="Index" asp-controller="Customer">
                    <span>View Customers</span>
                </a>

            </div>
        </div>

        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-bar-chart-line"></i>
                <span class="menu-text">Reports</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">

                <a asp-action="LeadReport" asp-controller="Reports">
                    <span>Lead Reports</span>
                </a>

                <a asp-action="FollowUpReport" asp-controller="Reports">
                    <span>Follow-up Reports</span>
                </a>

                <a asp-action="ConversionReport" asp-controller="Reports">
                    <span>Conversion Reports</span>
                </a>

                <a asp-action="EmployeeReport" asp-controller="Reports">
                    <span>Employee Reports</span>
                </a>

                <a asp-action="TeamReport" asp-controller="Reports">
                    <span>Team Reports</span>
                </a>

            </div>
        </div>

        <div class="menu-group">
            <a href="javascript:void(0)" class="menu-toggle">
                <i class="bi bi-gear-fill"></i>
                <span class="menu-text">Settings</span>
                <i class="bi bi-chevron-down ms-auto"></i>
            </a>

            <div class="submenu">
                <a asp-action="ForgotPassword" asp-controller="Auth">
                    <span>Change Password</span>
                </a>
            </div>
        </div>
    </div>
    <div id="overlay"></div>

    <!-- MAIN -->
    <div class="main">

        <!-- TOPBAR -->
        <div class="topbar d-flex justify-content-between align-items-center mb-4 px-3 py-2">

            <!-- LEFT -->
            <div class="d-flex align-items-center gap-3">

                <button class="menu-btn" onclick="toggleSidebar()">
                    <i class="bi bi-list"></i>
                </button>

                <div>
                    <h6 class="mb-0 text-secondary">SalesPulseCRM</h6>
                    <h5 class="mb-0 fw-semibold">Dashboard</h5>
                </div>

            </div>

            <!-- RIGHT -->
            <div class="d-flex align-items-center gap-3">

                <!-- Notification -->
                <div class="icon-btn position-relative">
                    <i class="bi bi-bell"></i>
                    <span class="badge-dot"></span>
                </div>

                <!-- User -->
                <div class="user-dropdown">

                    @if (!string.IsNullOrEmpty(userName) && userName != "User")
                    {
                        <div class="user-trigger d-flex align-items-center gap-2">
                            <div class="avatar">
                                @(userName.Length > 0 ? userName.Substring(0, 1) : "U")
                            </div>
                            <span>@userName</span>
                            <i class="bi bi-chevron-down"></i>
                        </div>

                        <div class="dropdown-menu-custom">

                            <a href="#">
                                <i class="bi bi-person"></i> Profile
                            </a>

                            <a asp-action="Logout" asp-controller="Auth">
                                <i class="bi bi-box-arrow-right"></i> Logout
                            </a>

                        </div>
                    }
                    else
                    {
                        <a asp-action="Login" asp-controller="Auth" class="btn btn-outline-light btn-sm">
                            <i class="bi bi-box-arrow-in-right"></i> Login
                        </a>
                    }

                </div>
              
               

            </div>

        </div>

        <!-- CONTENT -->
        <div>
            @RenderBody()
        </div>

    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const sidebar = document.getElementById("sidebar");
        const overlay = document.getElementById("overlay");
        const main = document.querySelector(".main");

        function toggleSidebar() {
            if (window.innerWidth <= 768) {
                sidebar.classList.toggle("active");
                overlay.classList.toggle("active");
            } else {
                sidebar.classList.toggle("collapsed");
                main.classList.toggle("collapsed");
            }
        }

        // 🔥 CLOSE on overlay click
        overlay.addEventListener("click", () => {
            sidebar.classList.remove("active");
            overlay.classList.remove("active");
        });

        // 🔥 CLOSE on menu click (mobile UX)
               document.querySelectorAll(".sidebar a:not(.menu-toggle)").forEach(link => {
            link.addEventListener("click", () => {
                if (window.innerWidth <= 768) {
                    sidebar.classList.remove("active");
                    overlay.classList.remove("active");
                }
            });
        });
                      document.addEventListener("DOMContentLoaded", function () {

            const trigger = document.querySelector(".user-trigger");
            const menu = document.querySelector(".dropdown-menu-custom");

            if (!trigger || !menu) return;

            // CLICK ON USER
            trigger.addEventListener("click", function (e) {
                e.stopPropagation(); // 🔥 MAIN FIX
                menu.classList.toggle("show");
            });

            // CLICK OUTSIDE
            document.addEventListener("click", function () {
                menu.classList.remove("show");
            });

        });

                        // Leads dropdown toggle
            document.querySelectorAll(".menu-toggle").forEach(toggle => {
            toggle.addEventListener("click", (e) => {
                e.preventDefault();   // 🔥 important
                const parent = toggle.parentElement;
                parent.classList.toggle("open");
            });
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>