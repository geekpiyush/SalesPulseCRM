@{
    ViewBag.Title = "SalesPulseCRM | Dashboard";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />
<link href="~/css/HomeController/HomeDashboard.css" rel="stylesheet" />

<div class="container-fluid dashboard">

    <!-- 🔥 TODAY TASKS (ACTIONABLE) -->
    <div class="row g-3 mb-4">

        <div class="col-12 col-sm-6 col-lg-3">
            <div class="card-box">

                <div class="card-icon bg-blue">
                    <i class="bi bi-list-check"></i>
                </div>

                <div class="w-100">
                    <div class="card-title">Today Tasks</div>
                    <div class="task-breakdown mt-2">

                        <div class="task-item clickable-row"
                             onclick="goToFollowups('today')">
                            <span>Followups</span>
                            <span class="count" id="followupsCount">0</span>
                        </div>

                        <div class="task-item clickable-row overdue"
                             onclick="goToFollowups('overdue')">
                            <span>Missed</span>
                            <span class="count" id="missedCount">0</span>
                        </div>

                        <div class="task-item clickable-row meeting"
                             onclick="goToMeetings()">
                            <span>Meetings</span>
                            <span class="count" id="meetingsCount">0</span>
                        </div>

                    </div>
                </div>

            </div>
        </div>

        <!-- 🔥 UNASSIGNED -->
        <div class="col-12 col-sm-6 col-lg-3">
            <div class="card-box">

                <div class="card-icon bg-red">
                    <i class="bi bi-person-x"></i>
                </div>

                <div class="w-100">

                    <div class="card-title">Unassigned Leads</div>

                    <!-- 🔥 MAIN VALUE -->
                    <div class="card-value clickable-card"
                         onclick="goToLeads('unassigned')">
                        34
                    </div>

                    <!-- 🔥 BREAKDOWN -->
                    <div class="unassigned-breakdown mt-2">

                        <div class="mini-item clickable-row"
                             onclick="goToLeads('unassigned-today')">
                            <span>Today</span>
                            <span>5</span>
                        </div>

                        <div class="mini-item clickable-row warning"
                             onclick="goToLeads('unassigned-old')">
                            <span>3+ Days</span>
                            <span>12</span>
                        </div>

                    </div>

                    <!-- 🔥 CTA -->
                    <div class="assign-cta mt-2"
                         onclick="goToLeads('unassigned')">
                        Assign Now →
                    </div>

                </div>

            </div>
        </div>

        <!-- 🔥 CONVERTED -->
        <div class="col-12 col-sm-6 col-lg-3">
            <div class="card-box">

                <div class="card-icon bg-green">
                    <i class="bi bi-check-circle"></i>
                </div>

                <div class="w-100">

                    <div class="card-title">Converted</div>

                    <!-- 🔥 MAIN VALUE -->
                    <div class="card-value clickable-card"
                         onclick="goToLeads('converted-month')">
                        12
                    </div>

                    <!-- 🔥 BREAKDOWN -->
                    <div class="converted-breakdown mt-2">

                        <div class="mini-item clickable-row"
                             onclick="goToLeads('converted-today')">
                            <span>Today</span>
                            <span>2</span>
                        </div>

                        <div class="mini-item clickable-row"
                             onclick="goToLeads('converted-week')">
                            <span>This Week</span>
                            <span>5</span>
                        </div>

                        <div class="mini-item clickable-row"
                             onclick="goToLeads('converted-month')">
                            <span>This Month</span>
                            <span>12</span>
                        </div>

                    </div>

                </div>

            </div>
        </div>

    </div>

    <!-- 🔥 FUNNEL (CLICKABLE) -->
    <div class="card-clean mb-4">
        <h5 class="mb-3">Lead Funnel</h5>

        <div class="funnel-inline">

            <div class="funnel-step clickable-card" onclick="goToLeads('new')">
                <div class="funnel-value">120</div>
                <div class="funnel-label">New</div>
                <div class="funnel-sub">100%</div>
            </div>

            <div class="funnel-arrow">→</div>

            <div class="funnel-step clickable-card" onclick="goToLeads('contacted')">
                <div class="funnel-value">80</div>
                <div class="funnel-label">Contacted</div>
                <div class="funnel-sub">66%</div>
            </div>

            <div class="funnel-arrow">→</div>

            <div class="funnel-step clickable-card" onclick="goToLeads('qualified')">
                <div class="funnel-value">45</div>
                <div class="funnel-label">Qualified</div>
                <div class="funnel-sub warning">56%</div>
            </div>

            <div class="funnel-arrow">→</div>

            <div class="funnel-step success clickable-card" onclick="goToLeads('closed')">
                <div class="funnel-value">12</div>
                <div class="funnel-label">Closed</div>
                <div class="funnel-sub success-text">26%</div>
            </div>

        </div>
    </div>

    <!-- 🔥 SECOND ROW -->
    <div class="row g-3">

        <!-- 👤 USERS -->
        <div class="col-12 col-lg-6">
            <div class="card-clean">
                <h5 class="mb-3">Assigned Leads</h5>

                <!-- 🔥 USER -->
                <div class="user-card clickable-row"
                     onclick="goToUserLeads(1)">

                    <div class="user-left">
                        <div class="user-avatar">P</div>

                        <div>
                            <div class="user-name">Piyush</div>
                            <div class="user-meta">
                                5 overdue • 2 today
                            </div>
                        </div>
                    </div>

                    <div class="user-right">
                        <div class="user-count">45</div>
                        <div class="user-label">leads</div>
                    </div>

                </div>

                <!-- 🔥 USER -->
                <div class="user-card clickable-row"
                     onclick="goToUserLeads(2)">

                    <div class="user-left">
                        <div class="user-avatar">R</div>

                        <div>
                            <div class="user-name">Rahul</div>
                            <div class="user-meta text-success">
                                All on track
                            </div>
                        </div>
                    </div>

                    <div class="user-right">
                        <div class="user-count">32</div>
                        <div class="user-label">leads</div>
                    </div>

                </div>

            </div>
        </div>

        <!-- ⏳ AGING -->
        <div class="col-12 col-lg-6">
            <div class="card-clean">
                <h5 class="mb-3">Lead Aging</h5>

                <!-- 🔥 SAFE -->
                <div class="aging-card clickable-row"
                     onclick="goToLeads('aging-0-2')">

                    <div class="aging-left">
                        <div class="aging-dot safe"></div>
                        <span>0-2 Days</span>
                    </div>

                    <div class="aging-right">
                        <span class="count">40</span>
                        <span class="status safe-text">Healthy</span>
                    </div>

                </div>

                <!-- 🔥 WARNING -->
                <div class="aging-card clickable-row"
                     onclick="goToLeads('aging-3-7')">

                    <div class="aging-left">
                        <div class="aging-dot warning"></div>
                        <span>3-7 Days</span>
                    </div>

                    <div class="aging-right">
                        <span class="count">25</span>
                        <span class="status warning-text">Attention</span>
                    </div>

                </div>

                <!-- 🔥 DANGER -->
                <div class="aging-card danger clickable-row"
                     onclick="goToLeads('aging-7+')">

                    <div class="aging-left">
                        <div class="aging-dot danger"></div>
                        <span>7+ Days</span>
                    </div>

                    <div class="aging-right">
                        <span class="count">18</span>
                        <span class="status danger-text">Critical</span>
                    </div>

                </div>

            </div>
        </div>

    </div>

    <!-- 🔥 TIMELINE -->
    <div class="card-clean mt-4">
        <h5 class="mb-3">Recent Activity</h5>

        <div id="dashboardTimeline">
            <!-- 🔥 PARTIAL LOAD -->
           
        </div>
    </div>

</div>
<script>
      fetch('/Lead/GetTimelinePartial?leadId=0')
    .then(res => res.text())
    .then(html => {
        document.getElementById('dashboardTimeline').innerHTML = html;
    });

    async function loadTodayTask()
    {
        try{
            const response = await fetch('/api/leadapi/today-tasks')

            if(!response.ok)
            {
                throw new Error("API Failed")
            }

            const data = await response.json();
         document.getElementById("followupsCount").innerText = data.followups;
        document.getElementById("missedCount").innerText = data.missed;
        document.getElementById("meetingsCount").innerText = data.meetings;
        }catch (error) {
        console.error("TodayTasks Error:", error);
    }
    }
    document.addEventListener("DOMContentLoaded", loadTodayTask);
</script>